#compdef unmark

_unmark() {

    # Use expansion instead of the following ls
    _arguments "1: :($(ls $MARKPATH))"

    # local allmarks=($MARKPATH/*(@N))
    # _arguments '1:marks:($allmarks:t)'
}

_unmark "$@"
